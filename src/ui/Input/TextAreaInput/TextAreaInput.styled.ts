import styled from 'styled-components'

import { theme } from 'config'

export const ValueLengthText = styled.small`
  font-size: ${theme.font.sizes.small};
  color: ${theme.colors.neutral[500]};
  margin-left: auto;
  margin-top: 1rem;
`
